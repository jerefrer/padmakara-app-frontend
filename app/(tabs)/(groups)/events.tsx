// Re-export the public events list screen under the (groups) stack
// so navigating to "Teachings & Talks" stays within the home tab
// instead of switching to a hidden (events) tab
export { default } from '../(events)/index';
